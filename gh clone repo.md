
## Interactive Login with Token

```bash
gh auth login --with-token
```

## Non-interactive (Scripted Login)

directly pipe token

```bash
echo YOUR_TOKEN | gh auth login --with-token
```

[[verify authentication | Verify Login Authentication]]
